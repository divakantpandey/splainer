"""SQL dialect-specific rendering.

Each dialect module implements the SQLDialect interface from base.py,
providing dialect-specific SQL syntax (identifier quoting, LIMIT vs TOP,
type mappings, etc.).

TODO: Add more dialects as needed.
"""
